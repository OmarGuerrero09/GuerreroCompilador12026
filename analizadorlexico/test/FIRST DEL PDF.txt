FIRST DEL PDF

First(<programa>) = First(<Bloque>)
                  = { const, var, proced, begin, id, write, read, call, if, while, for }

First(<Bloque>) = First(<Lin1>)
                = { const, ∅ }
                = { const } + First(<Lin2>)
                = { const } + { var, ∅ }
                = { const, var } + First(<Lin3>)
                = { const, var } + { proced, ∅ }
                = { const, var, proced } + First(<proposicion>)
                = { const, var, proced, begin, id, write, read, call, if, while, for }

First(<Lin1>)         = { const, ∅ }
First(<cicl1>)        = { id }
First(<cicl1_prima>)  = { , , ∅ }

First(<Lin2>)         = { var, ∅ }
First(<cicl2>)        = { id }
First(<cicl2_prima>)  = { , , ∅ }

First(<Lin3>)         = { proced, ∅ }

First(<proposicion>) = First(<multLin>)
                     = { begin, id, write, read, call, if, while, for }

First(<multLin>) = First(<LinBeg>) + First(<LinExp>) + First(<LinWrite>) + First(<LinRead>)
                 + First(<LinCall>) + First(<LinIf>) + First(<LinWhile>) + First(<LinFor>)
                 = { begin } + { id } + { write } + { read } + { call } + { if } + { while } + { for }
                 = { begin, id, write, read, call, if, while, for }

First(<LinBeg>)   = { begin }
First(<LinExp>)   = { id }
First(<LinWrite>) = { write }
First(<LinRead>)  = { read }
First(<LinCall>)  = { call }
First(<LinIf>)    = { if }
First(<LinWhile>) = { while }
First(<LinFor>)   = { for }

First(<ciclProp>) = First(<proposicion>)
                  = { begin, id, write, read, call, if, while, for }

First(<ciclProp_prima>) = { ; , ∅ }

First(<condicion>) = First(<Expresion>)
                   = { ( , id , num }

First(<mult>)    = { == , != , < , > , <= , >= }
First(<DisDown>) = { to , down }
First(<DisNum>)  = { id , num }

First(<Expresion>) = First(<Termino>)
                   = { ( , id , num }

First(<Exp_prima>)    = { + , - , ∅ }
First(<DisAsg>)       = { + , - }

First(<Termino>) = First(<factor>)
                 = { ( , id , num }

First(<Termino_prima>) = { * , / , ∅ }
First(<DisDiv>)        = { * , / }

First(<factor>) = { ( , id , num }

